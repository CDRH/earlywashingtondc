<h2>Annotated Cases</h2>

<p>Below is an index of the Petition for Freedom cases that have been annotated by project scholars, discussing details such as the individuals involved, the significance of the case, and important legal issues.</p>

<div class="pagination_container">
  <h2 class="sr-only" id="pagination_label">Pagination</h2>
  <nav aria-labelledby="pagination_label">
    <%= paginator(@total_pages) %>
  </nav>
</div>

<h1><%= "#{@total_found} annotated cases" %></h1>

<ul>
  <% @docs[:docs].each do |doc| %>
    <% label = "#{doc['title']}" %>
    <li>
      <%= link_to label, case_path(doc["id"]) %>
    </li>
  <% end %>
</ul>

<div class="pagination_container">
  <h2 class="sr-only" id="bottom_pagination_label">Bottom Pagination</h2>
  <nav aria-labelledby="bottom_pagination_label">
    <%= paginator(@total_pages) %>
  </nav>
</div>
