<h2>All Cases</h2>

<p>Below is an index of the Petition for Freedom cases tried before the Circuit Court for the District of Columbia between 1808 and 1859.</p>

<div class="jump_menu">
  <h4>Sort Results: </h4>
  <% sort = params[:sort] ? params[:sort] : "title" %>
  <%= link_to "Alphabetical", casesAll_path(sort("title")), :class => selected_sort?("title", sort) %>
  <%= link_to "Date", casesAll_path(sort("date")), :class => selected_sort?("date", sort) %>
</div>

<div class="pagination_container">
  <h2 class="sr-only" id="pagination_label">Pagination</h2>
  <nav aria-labelledby="pagination_label">
    <%= paginator(@total_pages) %>
  </nav>
</div>

<h1><%= "#{@total_found} cases" %></h1>


<ul>
  <% @docs[:docs].each do |doc| %>
    <% label = doc['title'] %>
    <% label += " (#{doc["dateDisplay"]})" if doc["dateDisplay"].present? %>
    <li>
      <%= link_to label, case_path(doc["id"]) %>
    </li>
  <% end %>
</ul>

<div class="pagination_container">
  <h2 class="sr-only" id="bottom_pagination_label">Bottom Pagination</h2>
  <nav aria-labelledby="bottom_pagination_label">
    <%= paginator(@total_pages) %>
  </nav>
</div>
